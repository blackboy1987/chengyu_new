import * as React from 'react';
import { View} from 'remax/one';
import './index.css';

import Account from "@/components/Account";

export default () => {
  return (
      <View className="page">
          <Account />
      </View>
  );
};
